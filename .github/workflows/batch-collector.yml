name: batch collector
on:
  schedule:
    - cron: 0 0 * * *
  workflow_dispatch: {}
  push:
    paths:
      - .github/workflows/batch-collector.yml
jobs:
    batch_print:
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                package: 
                  - haifa
                  - tel aviv
                  - jerusalem
        steps:
         - run: |
              echo "${{ matrix.package }}"
